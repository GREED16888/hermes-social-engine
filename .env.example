# Hermes Social Engine example environment
# Copy to .env locally if needed. Never commit real values.

# YouTube uploads default to private. Set only when intentionally changing visibility.
HSE_YOUTUBE_PRIVACY=private

# Optional Telegram provider env names
HSE_TELEGRAM_BOT_TOKEN=
HSE_TELEGRAM_CHAT_ID=
