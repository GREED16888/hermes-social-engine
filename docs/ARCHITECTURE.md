# Architecture

Hermes chat -> hse CLI -> SQLite -> scheduler -> provider adapter -> platform/local log.

Statuses: draft, scheduled, publishing, posted, failed.

V0 providers: dryrun, local_json. Future: YouTube, Bluesky, Meta, TikTok.
